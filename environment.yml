# Use this file to create a conda environment using:
# conda create -n <name> --file environment.yml

name: improver
channels:
  - conda-forge
dependencies:
  - python=3.6
  - iris=2.2
  - numpy=1.15.4
  - cftime=1.0.1
  - python-stratify=0.1
  - sphinx
  - pytest
  - pytest-cov
  - codacy-coverage
  - pylint=2.1.1
  - astroid=2.1.0
  - pycodestyle
  - filelock
  - mock
  - pip:
    - od
    - sigtools>=2.0
    - clize
